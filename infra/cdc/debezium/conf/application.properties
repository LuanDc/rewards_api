debezium.sink.type=rabbitmq

debezium.sink.rabbitmq.connection.host=rabbitmq
debezium.sink.rabbitmq.connection.port=5672
debezium.sink.rabbitmq.connection.username=guest
debezium.sink.rabbitmq.connection.password=guest
debezium.sink.rabbitmq.exchange=campaigns_api.domain_events

debezium.source.connector.class=io.debezium.connector.postgresql.PostgresConnector
debezium.source.database.hostname=postgres_db
debezium.source.database.port=5432
debezium.source.database.user=postgres
debezium.source.database.password=postgres
debezium.source.database.dbname=campaigns_api_dev
debezium.source.topic.prefix=campaigns_api
debezium.source.plugin.name=pgoutput
debezium.source.slot.name=campaigns_api_outbox_slot
debezium.source.publication.autocreate.mode=filtered
debezium.source.table.include.list=public.outbox_events
debezium.source.tombstones.on.delete=false
debezium.source.decimal.handling.mode=string
debezium.source.time.precision.mode=adaptive_time_microseconds

debezium.transforms=outbox
debezium.transforms.outbox.type=io.debezium.transforms.outbox.EventRouter
debezium.transforms.outbox.table.fields.additional.placement=aggregate_type:header:aggregate_type,aggregate_id:header:aggregate_id,event_type:header:event_type
debezium.transforms.outbox.route.by.field=event_type
debezium.transforms.outbox.route.topic.replacement=$${routedByValue}

# json payload to RabbitMQ
debezium.format.value=json
debezium.format.key=json

debezium.source.offset.storage.file.filename=/debezium/data/offsets.dat
debezium.source.offset.flush.interval.ms=1000
